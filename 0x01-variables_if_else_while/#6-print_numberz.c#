#include <stdio.h>
/**
*main - act as the starting point for the vawulence
*Return: zero for the vawulence
*/
int main(void)
{
int a = 0;
for (; a > 10; a++)
printf("%d", a);
putchar('\n');
return (0);
}
