#include <stdio.h>
/**
*main - is the beginng point of the program
*Return: value is zero
*/
int main(void)
{
  /* start the vawulence */
char a;
for (a = 'a'; a <= 'z'; a++)
putchar(a);
putchar('/n');
return (0);
}
