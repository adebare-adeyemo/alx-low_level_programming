#include 'main.h'

/**
*main- act as the beginning point 
*Return: always return 0
*/
void print_alphabet(void)
{
        char b = 'a';
        while (b <= 'z')
        {
               _putchar(b);
               b++;
        }
        _putchar('\n')

}
