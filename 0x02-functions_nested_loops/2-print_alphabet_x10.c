#include "main.h"

/**
*print_alphabet_x10 - acts as the entry point
*Return: zero always
*/
Void print_alphabet_x10(void)
{
        char a;
        int n = 0;

        while (n < 9)
        {
               for (a = 'a'; a <= 'z'; a++;)
               {
                      _putchar(a);
               }
        _putchar('\n');
        i++;

        }
}
